# 变更记录

本文件记录 mini_deploy 的重要变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号计划遵循[语义化版本](https://semver.org/lang/zh-CN/)。

项目尚未发布首个公开测试版本。发布前的兼容性和升级行为仍可能调整；所有破坏性变化都应在此说明。

## [Unreleased]

### 新增

- 新增本机/Docker Nginx 接入引导，检测运行容器、网络、端口及目录挂载，保存全局实例和每项目后端地址，提供连通检查和托管 HTTP 站点移除。Docker 域名和证书操作在已验证的容器 ID 内执行检查/重载，证书路径自动映射；不自动改动已有容器的挂载或运行模式。
- 新增 Docker Nginx Compose 初始示例及挂载变化、后端地址校验、实例切换保护和证书回滚测试。

- 新增网页 Nginx 证书管理：为已登记的业务项目上传 PEM 完整证书链和私钥、查看有效期、改名、替换、启用或停用 HTTPS，以及删除未使用的证书。证书在数据目录中私有保存，不回传私钥；应用前检查域名、有效期、密钥匹配和 Nginx 配置，应用失败时恢复旧配置并尝试重新加载。
- 新增页面切换和证书生命周期回归测试。

- 新增安全策略、贡献指南、行为准则以及标准化的 GitHub Issue 和 Pull Request 模板。
- 新增 `pytest`、Ruff 和 GitHub Actions 基线，CI 覆盖 Python 3.10-3.13、JavaScript 语法和 ShellCheck。
- 新增 GitHub Actions 与 Python 开发依赖的 Dependabot 周期更新配置。
- 新增仓库换行符规则，确保 Linux 脚本和配置在跨平台编辑后仍使用 LF。
- 新增服务器诊断、引导和安装状态备份脚本。
- 新增只读 `scripts/verify_backup.py`，可从 bundle 目录或 `installation.tar.gz` 校验 `format=2` 清单、摘要、归档范围、至少一份 root 私有项目配置、硬链接解析和 Agent 指纹；符号链接只统计且不跟随，不验证其未来恢复目标，也不执行解压或恢复。
- 新增部署成功、取消、超时、进程清理、worker 异常恢复和队列隔离的自动化回归测试。
- 新增管理员密码设置、Session 撤销和非交互密码输入命令。
- 新增只读 `agent.py validate-config` 命令，供直接编辑项目配置后在重启服务前执行与 Agent 启动一致的校验。

### 变更

- 面板固定监听 `0.0.0.0:6868`，安装后默认使用 `http://服务器公网IP:6868`，无需域名或 Nginx。安装器识别公网地址、尝试放行活动 UFW/firewalld 并提示云安全组配置；端口占用时停止安装。升级同步旧环境值和托管 Nginx 上游端口，保留原有域名/HTTPS 配置。旧的监听地址和端口环境变量不再覆盖固定入口。

- 安装器现在会在服务和 Nginx 公网入口启动前初始化管理员凭据；非交互安装缺少密码输入时会安全终止。
- 非交互管理员密码文件必须归 root 所有且不能向组或其他用户开放读取权限。
- systemd 单元和 Agent 环境会按实际的程序、配置、数据、日志和服务路径生成；已有安装路径冲突时会拒绝静默迁移并提示先备份。
- 安装完成提示会输出带实际环境文件、程序路径和服务名的参数化诊断及管理员命令，避免自定义布局误读或修改默认实例。
- `server.env` 的值现在会由服务器引导脚本正确导出给安装器。
- 非交互模式下，`SETUP_NGINX=auto` 不再沿用交互提示的默认答案安装 Nginx；只有显式设置为 `yes` 才会预先授权安装，`false` 会禁用 Nginx 配置。
- 安装器识别到已有安装时，会在配置的备份目录（默认 `/var/backups/mini-deploy-agent`）原子发布 root 私有 bundle；同一目录中的 `installation.tar.gz`、带 SHA-256 的 `manifest` 和 `complete` 作为一个整体出现，不再分别发布归档文件和旁置清单。安装器会在修改现有安装前自动校验新 bundle，失败即停止。项目配置、状态、环境文件及旧备份进入归档。安装器管理的独立日志目录原地保留且不作为单独归档输入，但旧布局或手工放在 `APP_HOME`、`DATA_HOME` 内的日志仍可能随目录进入归档。
- 手动安装备份现在显式接收并校验 `LOG_HOME`，拒绝程序、数据、日志和归档目录互相包含，并在清单中单独记录未作为归档输入的托管日志目录。
- 文档明确当前私密漏洞报告入口、备用安全联系方式和行为准则私密举报渠道尚未配置和验证，并将其列为首个公开版本发布前的外部仓库配置阻塞项。
- 安装目录新增 root 所有的安装标记；非空且无法识别的 `APP_HOME` 会被拒绝，避免 `rsync --delete` 清空无关目录。外观符合旧版但没有 app/systemd/Nginx 托管标记的目录或文件默认也会拒绝，只有显式临时启用 `DEPLOY_ALLOW_LEGACY_INSTALL_ADOPTION=true` 才会在原始状态 bundle 原子发布成功后接管。
- 安装器复制程序后会把 root 服务代码收紧为 `root:root` 且移除组/其他用户写权限，并拒绝不可信的控制文件、systemd unit、Nginx 配置和托管目录。
- Agent、安装与独立备份共用 `/run/mini-deploy-agent/maintenance.lock`：队列中的部署/项目回滚任务持共享锁，安装与备份持非阻塞独占锁；维护期间的新触发返回 HTTP `503`，配置、状态和审计写入等待。systemd 在服务重启时保留 root 私有运行目录并在服务器重启后重建，避免服务重启切换到不同锁文件。
- 备份 `manifest` 升级为可搬运的 format 2：归档名使用 bundle 内相对路径，并记录程序、环境、数据、日志、systemd、Nginx 的原始路径、旧版接管状态及 Agent 文件指纹，为后续严格恢复校验提供依据。
- 项目配置备份默认迁移到 `/var/lib/mini-deploy-agent/backups`，不再继续写入程序发布目录。
- 活动项目配置默认迁移到 `/var/lib/mini-deploy-agent/projects.json`。升级只自动处理旧默认路径，并在迁移前完成原始状态备份；验证备份后会停止仍在运行的旧 Agent，后续失败时仅尽力重新启动此前处于 active 的同名 systemd unit，避免迁移窗口中的最后一次配置写入丢失；这不会恢复已经修改的文件，也不构成版本回滚。首次迁移时新旧内容冲突、活动文件缺失或无法纳入当前归档格式的自定义路径会安全终止，不会用示例配置覆盖项目 Token。环境文件已选择数据目录后，以该副本为唯一权威配置，并只对可能过期的程序目录旧副本做文件安全检查。
- WebHook 接入说明统一使用 Gitee/GitLab 原生 Token 字段或 GitHub HMAC Secret，URL 只携带项目标识。

### 修复

- 修复安装器生成的 `WorkingDirectory` 包含字面双引号而导致 systemd `bad-setting`；无空格托管路径按原值输出，`ExecStart` 保留参数引用，并在替换服务文件之前用 `systemd-analyze verify` 校验，失败时保留原文件。

- 修复 Debian 等 GNU coreutils 环境安装时报 `chown: unrecognized option '--one-file-system'`：发布文件权限收紧和无 rsync 的复制路径改用 `find -xdev` 枚举后执行非递归 chown/chmod，保留嵌套挂载点预检查，并新增真实命令回归测试。

- 修复视图切换调用缺失函数的问题，补齐事件轮询、离开页面时停止轮询和通知配置加载；通知表单的未保存内容继续保留。

- 部署子进程在超时或取消后暂时无法退出时，Agent 会持续保留维护锁，并在该进程真正结束后精确清理生命周期；后续排队任务等待锁释放，不再被静默取出并丢弃。
- 标准输出关闭、编码失败或诊断日志异常不再中断部署 worker，也不会泄漏孤儿进程 reaper 持有的维护锁。

### 安全

- 默认拒绝 `?token=` 和 `?secret=` WebHook 鉴权；仅可通过显式的 `DEPLOY_ALLOW_QUERY_TOKEN=true` 临时开启旧集成兼容。
- 永久关闭公网网页管理员初始化，消除首次访问者抢先注册管理员的风险。
- UI Session Secret 不再回退复用 WebHook Secret，认证配置不完整时 Agent 拒绝启动。
- Agent HTTP 日志、审计日志和生成的 Nginx WebHook 日志会隐藏敏感查询参数。
- 环境文件和项目主配置使用唯一私有临时文件、`0600` 权限和原子替换；配置备份使用唯一文件名、`0600` 权限并在失败时清理。
- 安装备份目录必须归 root 所有；归档会先校验可读性，归档、清单和完成标记通过私有暂存目录一次性原子发布，控制文件和备份目录不允许符号链接绕过。
- 安装、升级和备份在递归操作前拒绝嵌套挂载点，并使用单文件系统模式限制 `rsync`、归档和所有权收紧；release、控制文件、标记、项目配置、凭据及备份产物同时拒绝多硬链接。
- root Shell 入口使用固定系统 `PATH`，服务器引导脚本会在读取可能含凭据的 `server.env` 前关闭 xtrace；安装与备份要求 Python 3.10 或更高版本。
- 项目配置修改使用统一事务锁，状态持久化按更新顺序串行写入，避免并发保存恢复旧 Secret 或旧状态快照。
- 登录限流在显式信任受控本机反向代理后，会按合法的真实客户端 IP 原子预留尝试次数；未知项目和错误 WebHook Secret 返回一致响应。
- 代理客户端 IP 信任改为显式配置：`DEPLOY_TRUST_LOOPBACK_PROXY_HEADERS=false` 默认忽略 `X-Real-IP`；仅开启后且 TCP 对端为 loopback 时才采纳合法地址。安装器生成的本机 Nginx 代理会覆盖该请求头，并自动启用此选项。
- 部署子进程不再继承管理员密码、Session Secret 或全局 WebHook Secret 等控制面凭据；其他业务环境变量保持不变。
- 示例项目不再包含公开固定 Token；项目保存时会生成强随机 Secret，启用项目拒绝缺失、过短或占位 Secret，损坏配置也不再静默回退到默认启用项目。

<!--
维护说明：
- 用户可感知的变化应记录在“新增 / 变更 / 弃用 / 移除 / 修复 / 安全”之一。
- 不要在这里记录纯格式化、内部注释或无行为影响的维护提交。
- 发布时把 Unreleased 内容移动到带日期的版本标题，并保留一个新的空 Unreleased 区域。
-->
