# 安全策略

mini_deploy 会接收来自公网的 WebHook，并在 Linux 服务器上执行项目部署脚本。安全问题可能直接影响服务器权限、项目代码和部署凭据。当前仓库尚未配置并验证可用的私密漏洞报告入口；请先阅读下方“漏洞报告渠道状态”，不要在公开内容中披露漏洞细节或真实凭据。

## 支持范围

项目当前处于首个公开测试版的准备阶段，尚未承诺稳定的 `v1.x` API 或配置格式。

- 首个版本发布前，安全修复只合入默认分支，不提供历史提交回补。
- 发布版本后，原则上只为最新发布版本提供安全更新；旧版本用户应先升级再验证问题。
- 默认分支属于开发版本，可能包含尚未完成验证的变更，不建议直接用于生产环境。
- mini_deploy 仅以 Linux 服务器为目标环境，要求 Python 3.10 或更高版本和 systemd，当前 CI 验证 3.10-3.13；Nginx 和 Docker Compose 为可选集成。Windows 不是受支持的部署目标。

发布说明如对支持范围有不同约定，以对应版本的发布说明为准。

## 当前安全边界

部署 mini_deploy 前，请确认以下边界：

- Agent 固定监听 `0.0.0.0:6868`，默认支持公网 IP 加端口直接访问。安装器会尝试放行系统防火墙中的 TCP 6868，云安全组需要单独配置。
- 默认入口使用 HTTP，不提供传输加密；需要加密时可配置额外的域名 HTTPS 入口，也可在安全组限制访问来源。
- 默认安装器当前明确以 `root` 运行 Agent。被授权的部署脚本也可以修改服务、Nginx 配置和项目目录；权限拆分完成前，只应在受控的专用测试服务器使用，并把部署脚本视为受信任的服务器代码。
- mini_deploy 不负责证明业务项目的 `deploy.sh`、依赖安装命令、数据库迁移或第三方镜像是安全的。
- 每个项目应使用独立的 WebHook Secret；UI 密码、Session Secret 和通知凭据不应复用。
- 不要把生产凭据写入仓库、Issue、Pull Request、截图或公开日志。

违反以上部署要求造成的问题仍可反馈，但不一定被认定为 mini_deploy 本身的安全漏洞。

## 安全默认值和数据位置

- WebHook URL 只应包含项目标识。`?token=` 和 `?secret=` 鉴权默认拒绝；Gitee/GitLab 使用平台原生 Token 字段，GitHub 使用 Secret 生成的 HMAC-SHA256 签名。
- 每个启用项目的 WebHook Secret 至少需要 32 位。留空保存时面板会生成 64 位十六进制随机 Token；示例占位值和过短值不会被启用。
- `DEPLOY_ALLOW_QUERY_TOKEN=true` 仅用于无法立即迁移的旧集成。URL 凭据仍可能进入外部负载均衡、CDN 或监控系统，因此应尽快关闭并轮换相关 Token。
- `DEPLOY_TRUST_LOOPBACK_PROXY_HEADERS=false` 默认忽略 `X-Real-IP`，登录限流和 WebHook 日志使用 TCP 对端地址。使用会覆盖该请求头的受信任本机反向代理时，可以显式设为 `true`；即使开启，公网直连等非 loopback 对端发送的转发头仍不会被信任。
- 管理员凭据必须在服务器终端或安装阶段初始化，网页不提供初始化入口。`DEPLOY_UI_SESSION_SECRET` 必须独立设置，不能复用 WebHook Secret。
- 管理员密码变更和 Session 撤销在重启 Agent 后生效；两种操作都会使旧 Session 失效。
- 部署脚本默认不会继承管理员密码、Session Secret 或全局 WebHook Secret。业务部署所需凭据仍应通过项目自己的受限环境文件或 Secret 管理方式提供。
- 环境文件和 `projects.json` 包含 Secret，应保持 `0600` 权限。活动项目配置默认位于 `/var/lib/mini-deploy-agent/projects.json`，状态、审计数据及项目配置备份也位于 `/var/lib/mini-deploy-agent`，运行日志位于 `/var/log/mini_deploy`。升级只自动迁移旧默认路径；首次迁移时新旧副本冲突或使用外置自定义路径会安全终止，避免猜错并覆盖 Token。环境文件已明确指向数据目录后，该副本是唯一权威配置，程序目录中的 root 私有旧副本仅作为可能过期的恢复证据保留。
- 优先通过面板修改项目。直接编辑 `projects.json` 后，应以显式 `DEPLOY_PROJECTS_FILE` 运行 `agent.py validate-config`，重新确认 `root:root`/`0600`，再重启 Agent；运行中的进程不会自动监视外部文件变化。
- 安装或升级备份默认位于 `/var/backups/mini-deploy-agent`。每次成功备份会原子发布一个 root 私有 bundle，目录内包含 `installation.tar.gz`、带 SHA-256 的 `format=2` 清单 `manifest` 和 `complete`；只有三个文件同时存在才视为完整。bundle 可能包含完整环境文件和 Secret，必须按生产凭据备份处理。安装器管理的独立日志目录默认位于 `/var/log/mini_deploy`，会原地保留且不会作为单独目录加入归档；但备份脚本会整体归档 `APP_HOME` 和 `DATA_HOME`，旧布局或手工放入这两个目录的日志仍可能进入归档。
- 安装器在修改已有安装前会自动执行 `python3 scripts/verify_backup.py <bundle-or-archive>`，校验失败即停止。该工具只读检查 bundle 私有权限和控制文件、`format=2` 清单、SHA-256、归档根、硬链接解析、Agent 指纹及安全上限；符号链接只会被统计而不会被跟随，归档成员也不能继续穿过链接写入，但符号链接将来的恢复目标不在校验范围内。该工具不会解压、恢复或修改备份，校验通过不能替代隔离环境的恢复演练。
- Agent、安装器和独立备份共用 `/run/mini-deploy-agent/maintenance.lock`。部署或项目回滚任务从进入队列起到结束都持共享锁；安装和备份使用非阻塞独占锁，因此已有任务或维护操作会使新维护操作直接失败。独占维护期间，新 WebHook、手动部署和项目回滚触发返回 HTTP `503`，配置、状态和审计写入等待维护结束。
- systemd 以 `0700` 的 `RuntimeDirectory` 管理 `/run/mini-deploy-agent`，并通过 `RuntimeDirectoryPreserve=yes` 在服务重启时保留它，避免维护过程重启服务后绕过原锁；服务器重启后会重新创建该目录。不要在这个临时运行目录存放状态、配置、日志或备份。

当前安装器会在覆盖程序文件前创建并校验备份，但版本目录原子切换、自动失败回滚、恢复、版本回滚和卸载命令仍在开发中。生产升级前应额外保留服务器快照，并在隔离环境中验证恢复方案。

## 漏洞报告渠道状态

请勿为未修复漏洞创建公开 Issue、Discussion 或 Pull Request。

当前仓库没有已经配置并验证的私密漏洞报告入口，暂时无法通过本文档接收机密漏洞细节。这是首个公开版本发布前必须完成的外部仓库配置阻塞项，不代表项目已经具备可用的私密报告流程。

正式公开发布前，仓库维护者必须启用并实测代码托管平台的私有漏洞报告功能，并在此处补充经过验证的备用私密联系方式。GitHub Private Vulnerability Reporting 可用时，仓库的 **Security** 页面会显示 **Report a vulnerability**。在入口实际出现并由维护者验证以前，请勿假定它可用，也不要改用公开 Issue、Discussion 或 Pull Request 发送漏洞细节。

当前如需提醒维护者建立安全联系方式，只能创建不包含漏洞类型、影响、复现步骤、环境信息或任何凭据的普通 Issue；待本节公布可验证的私密入口后，再通过该入口提交完整报告。

报告中建议包含：

- 受影响的版本、提交号和安装方式。
- Linux 发行版、Python 版本，以及是否使用 Nginx、Docker Compose。
- 漏洞影响和利用所需前提。
- 最小、无破坏性的复现步骤或概念验证。
- 已知的缓解措施或修复建议。
- 是否存在公开利用、主动攻击或即将披露的计划。

请使用占位符替换所有真实数据，例如 `<webhook-secret>`、`<session-cookie>`、`<server-ip>` 和 `<private-repository>`。如果真实凭据已经泄露，请立即轮换；不要把凭据发给维护者作为证明。

## 渠道启用后的处理流程

私密渠道正式启用后，维护者将尽力按以下目标处理通过该渠道收到的完整报告：

1. 在 5 个工作日内确认收到报告。
2. 在 10 个工作日内完成初步复现、影响判断和优先级评估。
3. 与报告者协调修复、公告和披露时间；修复可用前请保持漏洞细节非公开。
4. 修复发布后在 `CHANGELOG.md` 或安全公告中说明受影响版本、升级方法和必要的凭据轮换操作。

这些时间是维护目标，不是服务等级承诺。主动利用、远程代码执行、认证绕过、Secret 泄漏和破坏性升级问题会优先处理。

## 建议报告的问题

以下问题通常属于安全报告范围：

- 登录、Session、CSRF 或权限检查绕过。
- WebHook Token/HMAC 校验绕过、重放导致的越权部署。
- 命令注入、路径穿越、任意文件读写或任意项目操作。
- Token、密码、Cookie、通知凭据或私有仓库凭据泄漏。
- 未经授权的部署、取消、回滚、配置修改或日志下载。
- 安装、升级、恢复或卸载流程意外删除用户配置、项目代码或持久数据。
- 可由普通面板用户触发的容器、systemd 或 Nginx 权限提升。

以下情况通常不作为安全漏洞处理：

- 已获服务器管理员授权的恶意或错误 `deploy.sh` 所造成的行为。
- 服务器网络或云安全组配置导致的访问不可达。
- 仅缺少某个安全响应头，但无法证明实际安全影响。
- 只影响不再支持的旧版本，且在最新版本中无法复现。
- 第三方 Git 平台、Nginx、Docker、操作系统或业务项目自身的漏洞。
- 不包含可验证影响的自动扫描结果。

如果无法确定问题是否在范围内，请在私密渠道正式启用后通过该渠道报告；当前阶段不要在公开模板中提交漏洞细节。

## 安全研究准则

善意研究应只使用研究者拥有或明确获准测试的环境，避免访问他人数据、影响真实部署、持久化控制、横向移动和拒绝服务。在获取足以证明问题的最小证据后应停止测试并及时报告。

对于遵守本策略、尽量减少影响并善意协调披露的研究，项目维护者不会主动寻求法律追责。
